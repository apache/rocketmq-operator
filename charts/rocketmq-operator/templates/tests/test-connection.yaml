apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rocketmq-operator.fullname" . }}-test-connection"
  labels:
    {{- include "rocketmq-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rocketmq-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
